# 第14章 知识库RAG系统使用指南

## 📋 目录

1. [系统概述](#系统概述)
2. [快速开始](#快速开始)
3. [功能说明](#功能说明)
4. [操作流程](#操作流程)
5. [常见问题](#常见问题)

---

## 系统概述

第14章实现了一个完整的**知识库RAG（检索增强生成）智能问答系统**，具备以下核心功能：

✅ **用户认证系统**：注册、登录、JWT Token认证  
✅ **知识库管理**：创建、查看、删除知识库  
✅ **文档管理**：上传、查看、删除文档（支持PDF/Word/TXT/Markdown）  
✅ **智能问答**：基于知识库文档的RAG问答  
✅ **异步处理**：文档向量化异步处理，不阻塞用户操作

---

## 快速开始

### 1. 启动后端服务

```bash
# 进入后端目录
cd SpringBootAIProject/chapter_14

# 确保 MySQL 已启动并创建数据库
mysql -u root -p
CREATE DATABASE knowledge_base_rag;

# 配置环境变量（通义千问 API Key）
export DASHSCOPE_API_KEY=your_api_key_here

# 启动服务
mvn spring-boot:run
```

后端服务将在 **http://localhost:8014** 启动

### 2. 启动前端应用

```bash
# 进入前端目录
cd AIIntelligentCustomerService

# 安装依赖（首次）
npm install

# 启动开发服务器
npm run dev
```

前端应用将在 **http://localhost:5173** 启动

### 3. 访问系统

在浏览器中打开 `http://localhost:5173`，点击顶部导航栏的 **"第14章 - 知识库RAG"** 标签。

---

## 功能说明

### 🔐 1. 用户认证

#### 注册新账号

1. 首次访问第14章页面时，会自动跳转到登录页
2. 点击 **"注册"** 标签切换到注册界面
3. 填写以下信息：
   - **用户名**：唯一标识，不可重复
   - **邮箱**：用于找回密码（可选）
   - **密码**：至少6位字符
   - **确认密码**：必须与密码一致
4. 点击 **"注册"** 按钮
5. 注册成功后，会自动切换到登录界面

#### 登录账号

1. 在登录界面填写：
   - **用户名**
   - **密码**
2. 点击 **"登录"** 按钮
3. 登录成功后，会自动跳转到之前访问的页面
4. 顶部导航栏会显示用户名和 **"退出"** 按钮

#### 退出登录

点击顶部导航栏右上角的 **"退出"** 按钮即可退出登录。

---

### 📚 2. 知识库管理

#### 创建知识库

1. 登录后，进入 **"知识库管理"** 页面（点击右上角 **"管理"** 按钮）
2. 点击右上角的 **"+ 新建知识库"** 按钮
3. 在弹窗中填写：
   - **知识库名称**（必填）：如 "产品手册"、"技术文档"
   - **描述**（可选）：如 "包含所有产品相关文档"
4. 点击 **"创建"** 按钮

#### 查看知识库

- 左侧面板显示所有知识库列表
- 每个知识库显示：
  - 📚 图标
  - 知识库名称
  - 文档数量
  - 向量块数量

#### 选择知识库

点击左侧列表中的任意知识库，右侧会显示该知识库的详细信息和文档列表。

#### 删除知识库

1. 点击知识库右侧的 **🗑️** 图标
2. 确认删除操作
3. **注意**：删除知识库会同时删除其中的所有文档，且不可恢复！

---

### 📄 3. 文档上传管理

#### 上传文档

1. 在知识库管理页面，选择一个知识库
2. 点击右上角的 **"📤 上传文档"** 按钮
3. 选择要上传的文件（支持多选）
4. 系统会自动上传并开始处理

**支持的文件格式：**
- PDF（.pdf）
- Word（.docx、.doc）
- 纯文本（.txt）
- Markdown（.md）

**文件大小限制：** 单个文件最大 10MB

#### 文档处理流程

上传后，文档会经历以下状态：

1. **等待处理**（pending）：文档已上传，等待后台处理
2. **处理中**（processing）：正在提取文本并向量化
3. **已完成**（completed）：处理完成，可用于问答
4. **失败**（failed）：处理失败，查看错误信息

**处理时间：** 取决于文档大小和内容，通常几秒到几分钟不等。

#### 查看文档列表

选择知识库后，右侧会显示该知识库中的所有文档：

- 文件图标（📕 PDF、📘 Word、📄 TXT、📝 MD）
- 文件名
- 文件大小
- 分块数量
- 上传时间
- 处理状态

#### 删除文档

1. 点击文档右侧的 **🗑️** 图标
2. 确认删除操作
3. 文档删除后，相关向量数据也会被清除

---

### 💬 4. 智能问答

#### 开始对话

1. 返回聊天页面（点击 **"← 返回聊天"** 按钮）
2. 左侧选择一个知识库
3. 确保知识库中至少有一个状态为 **"已完成"** 的文档
4. 在底部输入框输入问题
5. 按 **Enter** 或点击 **"发送"** 按钮

#### 问答原理（RAG）

系统采用 **检索增强生成（RAG）** 技术：

1. **问题向量化**：将您的问题转换为向量
2. **相似度检索**：在知识库中查找最相关的文档片段（Top-5）
3. **上下文增强**：将相关文档作为上下文提供给AI
4. **生成回答**：AI基于上下文生成准确的回答

#### 示例问题

```
✅ 这个知识库包含哪些主要内容？
✅ 请总结一下文档的核心要点
✅ [具体的业务问题]（取决于您上传的文档内容）
```

#### 切换知识库

点击左侧不同的知识库，会自动清空当前对话，切换到新知识库。

#### 清空对话

点击右上角的 **"清空对话"** 按钮，可清空当前会话历史。

---

## 操作流程

### 完整使用流程示例

```
1. 注册/登录
   ↓
2. 创建知识库（例如："公司产品手册"）
   ↓
3. 上传文档（例如：产品介绍.pdf、使用指南.docx）
   ↓
4. 等待文档处理完成（状态变为"已完成"）
   ↓
5. 返回聊天页面
   ↓
6. 选择该知识库
   ↓
7. 开始提问（例如："这个产品的主要功能是什么？"）
   ↓
8. 查看AI基于文档的回答
```

---

## 常见问题

### Q1: 登录后刷新页面需要重新登录吗？

**A:** 不需要。系统使用 JWT Token 存储在 localStorage，只要不清除浏览器数据，刷新页面会自动保持登录状态。Token 有效期为 7 天。

### Q2: 上传文档后一直显示"处理中"？

**A:** 
- 检查后端日志是否有错误
- 确认通义千问 API Key 配置正确
- 大文件可能需要较长处理时间（几分钟）
- 系统会每 3 秒自动刷新状态

### Q3: 为什么AI回答的内容与文档不符？

**A:** 可能的原因：
- 文档尚未处理完成（检查状态是否为"已完成"）
- 相似度阈值设置过低（默认 0.3）
- 文档内容与问题关联性不强
- 建议上传更多相关文档以提高准确性

### Q4: 支持哪些文件格式？

**A:** 
- ✅ PDF（.pdf）
- ✅ Word（.docx、.doc）
- ✅ 纯文本（.txt）
- ✅ Markdown（.md）
- ❌ 暂不支持：图片、音频、视频、Excel

### Q5: 文档大小有限制吗？

**A:** 是的，单个文件最大 10MB。如果文件过大，建议：
- 拆分成多个小文件
- 提取关键内容制作精简版
- 转换为纯文本格式以减小体积

### Q6: 可以同时处理多个文档吗？

**A:** 可以。上传时支持多选文件，系统会并行处理多个文档。

### Q7: 如何提高问答准确性？

**建议：**
1. 上传高质量、结构清晰的文档
2. 文档内容要与问题领域相关
3. 上传更多相关文档以增加知识覆盖
4. 提问时尽量具体、明确
5. 避免过于宽泛或模糊的问题

### Q8: 删除知识库后能恢复吗？

**A:** 不能。删除操作是永久性的，会同时删除知识库和其中的所有文档、向量数据。请谨慎操作。

### Q9: 多个用户的数据会混淆吗？

**A:** 不会。每个用户的知识库和文档是完全隔离的，只能访问和管理自己创建的内容。

### Q10: 如何查看后端API文档？

**A:** 启动后端服务后，访问 Swagger 文档：
```
http://localhost:8014/swagger-ui/index.html
```

---

## 技术支持

如遇到其他问题，可以：

1. 查看后端日志：`SpringBootAIProject/chapter_14/logs`
2. 查看浏览器控制台错误信息
3. 检查 MySQL 数据库连接
4. 验证通义千问 API Key 是否有效

---

## 总结

第14章知识库RAG系统是一个功能完整的企业级应用，展示了：

- 🔐 完整的用户认证与权限管理
- 📄 多格式文档解析与向量化
- 🧮 基于余弦相似度的向量检索
- 🤖 RAG检索增强生成技术
- ⚡ 异步任务处理机制
- 🎨 现代化的前端界面设计

希望这个系统能帮助您更好地理解和应用 RAG 技术！🚀
